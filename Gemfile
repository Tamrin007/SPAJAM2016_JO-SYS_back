source "https://rubygems.org"
ruby '2.1.5'

# Sinatra
gem 'sinatra'

# Unicorn
gem 'unicorn'

# ActiveRecord
gem 'sinatra-activerecord'
gem 'activerecord'

group :development, :test do
	# Build tool
	gem 'rake'
	# SQLite
	gem 'sqlite3'
	# Foreman
	gem 'foreman'
end

group :production do
	# PostgreSQL
	gem 'pg'
end
